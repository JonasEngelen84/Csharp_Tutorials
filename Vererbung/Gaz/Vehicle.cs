using System;

namespace Vererbung.Gaz
{
    class Vehicle
    {
        public Vehicle(string registrationNumber)
        {
            Console.WriteLine($"Vehicle is being inizialized with number {registrationNumber}");
        }
    }
}
